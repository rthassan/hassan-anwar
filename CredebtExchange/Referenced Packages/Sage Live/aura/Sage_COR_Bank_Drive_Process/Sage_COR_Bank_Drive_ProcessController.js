({
	handleProcess : function(component, event, helper) {
		helper.process(component, event, helper);
	},
    handleFormLoad : function(component, event, helper) {
		helper.formLoad(component, event, helper);
	},
    handleViewSchedule : function(component, event, helper) {
		helper.viewSchedule(component, event, helper);
	},
    handleViewExecution : function(component, event, helper) {
		helper.viewExecution(component, event, helper);
	},
    handleViewTransactions : function(component, event, helper) {
		helper.viewTransactions(component, event, helper);
	}
})