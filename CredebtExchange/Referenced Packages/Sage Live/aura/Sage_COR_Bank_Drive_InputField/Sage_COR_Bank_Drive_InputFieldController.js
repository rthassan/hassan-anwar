({
	handleCheckValue : function(component, event, helper) {
		helper.checkValue(component, event, helper);
	},
	handleSetInvalid : function(component, event, helper) {
        helper.setInvalid(component, event, helper);
    },
    handleSetValid : function(component, event, helper) {
        helper.setValid(component, event, helper);
    }
})