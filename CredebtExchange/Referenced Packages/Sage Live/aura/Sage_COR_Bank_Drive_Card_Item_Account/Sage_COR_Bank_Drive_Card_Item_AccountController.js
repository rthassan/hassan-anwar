({
    handleToggleButtons : function(component, event, helper) {
		helper.toggleButtons(component, event, helper);
	},
    handleDeleteModal : function(component, event, helper) {
		helper.deleteModal(component, event, helper);
	},
    handleEditModal : function(component, event, helper) {
		helper.editModal(component, event, helper);
	},
	handleResetModal : function(component, event, helper) {
    		helper.resetModal(component, event, helper);
    	}
})